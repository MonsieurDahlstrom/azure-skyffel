import { ResourceGroup } from '@pulumi/azure-native/resources';
import {
  VirtualNetwork,
  Subnet,
  PrivateZone,
} from '@pulumi/azure-native/network';
export function createAks(input) {
  // Create AKS cluster
}
//# sourceMappingURL=index.js.map
